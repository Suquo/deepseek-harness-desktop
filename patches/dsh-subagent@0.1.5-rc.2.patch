diff --git a/lib/index.js b/lib/index.js
index 8be6ef7003ffb6dbb6838b47274b752f15685677..183f8c78cb55dd0618316bb240e6101fa461cee2 100644
--- a/lib/index.js
+++ b/lib/index.js
@@ -301,6 +301,7 @@ function observeRun(emit, provider, parent, run) {
 		emit("subagent/end", {
 			...identity,
 			stopReason: result.stopReason,
+			...result.diagnostic === void 0 ? {} : { diagnostic: result.diagnostic },
 			...result.output.length === 0 ? {} : { lastAssistantMessage: result.output }
 		}, parent);
 	}, () => {
@@ -332,7 +333,10 @@ function createActivationObserver(emit, provider, childId, parent) {
 	};
 	let boundary = SessionLogOffset(0);
 	let captured = { stopReason: "completed" };
-	const terminal = (failure) => failure === void 0 ? captured : { stopReason: "error" };
+	const terminal = (failure) => failure === void 0 ? captured : {
+		stopReason: "error",
+		diagnostic: subagentFailureDiagnostic(failure, childId)
+	};
 	return {
 		start: (child) => {
 			boundary = child.session.seq;
@@ -342,16 +346,17 @@ function createActivationObserver(emit, provider, childId, parent) {
 			const own = child.session.snapshotEvents(boundary);
 			const output = finalAssistantOutput(own);
 			captured = {
-				stopReason: epochStopReason(own),
+				...epochTerminal(own, childId),
 				...output === void 0 ? {} : { output }
 			};
 		},
 		terminal,
 		settle: (failure) => {
-			const { stopReason, output } = terminal(failure);
+			const { stopReason, diagnostic, output } = terminal(failure);
 			emit("subagent/end", {
 				...identity,
 				stopReason,
+				...diagnostic === void 0 ? {} : { diagnostic },
 				...output === void 0 ? {} : { lastAssistantMessage: output }
 			}, parent);
 		}
@@ -370,25 +375,35 @@ function createActivationObserver(emit, provider, childId, parent) {
 * failure still wins over a cancellation — stopping a child that had already
 * failed does not turn its failure into a cancellation.
 * @param events - this epoch's own event suffix.
-* @returns its terminal stop reason; `completed` only for an epoch that both
-*   closed cleanly and had nothing left to run.
+* @param childId - the durable child session id named in any failure diagnostic.
+* @returns its terminal facts; `completed` only for an epoch that both closed
+*   cleanly and had nothing left to run.
 */
-function epochStopReason(events) {
+function epochTerminal(events, childId) {
 	const { end, droppedUnrun } = foldConsumedWork(events);
 	switch (end?.data.reason.kind) {
-		case "max-tokens": return "max-tokens";
+		case "max-tokens": return { stopReason: "max-tokens" };
 		case "aborted":
-		case "interrupted": return "aborted";
-		case "error": return "error";
-		case "blocked": return "refusal";
+		case "interrupted": return { stopReason: "aborted" };
+		case "error": return {
+			stopReason: "error",
+			diagnostic: subagentFailureDiagnostic(end.data.reason.error, childId)
+		};
+		case "blocked": return { stopReason: "refusal" };
 		case void 0:
-		case "completed": return droppedUnrun ? "aborted" : "completed";
+		case "completed": return { stopReason: droppedUnrun ? "aborted" : "completed" };
 		/* v8 ignore next 3 -- `TurnEndReason` is merge-extensible, so this arm needs a
 		* backend that adds a variant; treating an unnameable reason as success would
 		* report failed work as completed. */
-		default: return "error";
+		default: return { stopReason: "error" };
 	}
 }
+/** Render one child failure through the same bounded contract as a one-shot run. */
+function subagentFailureDiagnostic(failure, childId) {
+	const code = typeof failure?.code === "string" && failure.code.length > 0 ? failure.code : "UNKNOWN";
+	const message = typeof failure?.message === "string" && failure.message.length > 0 ? failure.message : "no failure message recorded";
+	return limitSubagentDiagnostic(`${code} — ${message} (child session ${childId})`);
+}
 /** Render any listener-thrown value without letting coercion escape containment. */
 function renderThrown(value) {
 	try {
@@ -664,7 +679,10 @@ function createSettlementMessage(childId, terminal) {
 		content: [{
 			type: "text",
 			text: summary
-		}, ...terminal.output === void 0 ? [{
+		}, ...terminal.diagnostic === void 0 ? [] : [{
+			type: "text",
+			text: `Diagnostic: ${terminal.diagnostic}`
+		}], ...terminal.output === void 0 ? [{
 			type: "text",
 			text: "It left no closing message."
 		}] : [{
@@ -3228,4 +3246,4 @@ let SubagentRuntime = (() => {
 	};
 })();
 //#endregion
-export { AssistantOutputFold, NO_START_CAPABILITIES, SUBAGENT_DESCRIPTOR_VERSION, SubagentDepthError, SubagentError, SubagentRunId, SubagentRuntime, SubagentRuntime as default, appendDelegatedPolicyOverrides, applyChildComposition, assertPositiveFinite, assertSubagentMaxDepth, assertUsableCwd, captureDelegatedPolicyOverrides, childSessionMeta, delegationDepthOf, finalAssistantOutput, foldSubagentDescriptor, parentAgentOptionsForDelegation, resolveChildAgentOptions, resolveChildCwd, resolveChildDepth, settleRun, settleRunResult, snapshotSubagentDescriptor, subprocessRunHandle, validateConfiguredCwd };
+export { AssistantOutputFold, NO_START_CAPABILITIES, SUBAGENT_DESCRIPTOR_VERSION, SubagentDepthError, SubagentError, SubagentRunId, SubagentRuntime, SubagentRuntime as default, appendDelegatedPolicyOverrides, applyChildComposition, assertPositiveFinite, assertSubagentMaxDepth, assertUsableCwd, captureDelegatedPolicyOverrides, childSessionMeta, delegationDepthOf, finalAssistantOutput, foldSubagentDescriptor, limitSubagentDiagnostic, parentAgentOptionsForDelegation, resolveChildAgentOptions, resolveChildCwd, resolveChildDepth, settleRun, settleRunResult, snapshotSubagentDescriptor, subprocessRunHandle, validateConfiguredCwd };
diff --git a/lib/types/lifecycle.d.ts b/lib/types/lifecycle.d.ts
index 9d63a61b2497011fd4aba679ee480f2aacb3d245..a1b63f342762f55f9d659eade481158c29e9218b 100644
--- a/lib/types/lifecycle.d.ts
+++ b/lib/types/lifecycle.d.ts
@@ -25,6 +25,8 @@ import type { SubagentResult, SubagentRun, SubagentRunEndInfo, SubagentRunInfo }
 export interface ActivationTerminal {
     /** Why this epoch's last ordinary turn ended, or `error` when teardown failed. */
     readonly stopReason: SubagentResult['stopReason'];
+    /** Bounded child failure detail, absent when the Activation did not fail. */
+    readonly diagnostic?: string;
     /** The epoch's final assistant content, absent when it produced none or failed. */
     readonly output?: ContentBlock[];
 }
diff --git a/lib/types/out-of-process.d.ts b/lib/types/out-of-process.d.ts
index 5fcf4585783053fdd224d200fb31e3acd09eee1f..14a9673432061fed9a9f4f486eb3c131138f5d84 100644
--- a/lib/types/out-of-process.d.ts
+++ b/lib/types/out-of-process.d.ts
@@ -19,6 +19,15 @@ import type { SubagentCapabilities, SubagentResult, SubagentRun, SubagentStopRea
  * request needing any of them before `start` runs — never accepted-then-ignored.
  */
 export declare const NO_START_CAPABILITIES: SubagentCapabilities;
+/**
+ * Limit provider-authored failure detail to {@link SubagentResult.diagnostic}'s
+ * documented 4096-byte budget without splitting a UTF-8 sequence. Exported so
+ * IN-PROCESS backends bound their diagnostics through the same seam-owned rule
+ * the out-of-process settlement already applies, instead of restating it.
+ * @param diagnostic - safe diagnostic text produced by the provider.
+ * @returns the original text, or a visibly truncated value within the limit.
+ */
+export declare function limitSubagentDiagnostic(diagnostic: string): string;
 /**
  * Assert a configured timing bound is a positive finite number (it bounds a
  * teardown or shutdown wait; zero, negative, or NaN would skip or wedge it).
diff --git a/lib/types/types.d.ts b/lib/types/types.d.ts
index 77d6a72ed34eeee4260381cec61771bf969922e5..023e70fd23284cf4028be93b315a4d0b68afa635 100644
--- a/lib/types/types.d.ts
+++ b/lib/types/types.d.ts
@@ -101,6 +101,8 @@ export interface SubagentRunEndInfo {
     readonly local: boolean;
     /** The terminal stop reason. */
     readonly stopReason: SubagentResult['stopReason'];
+    /** Bounded child failure detail, absent when the run did not fail. */
+    readonly diagnostic?: string;
     /**
      * The child's final assistant output, selected by the same rule as
      * {@link SubagentResult.output}; absent on infrastructure rejection or when
