diff --git a/lib/index.js b/lib/index.js
index dbaf6a4..1e61ba9 100644
--- a/lib/index.js
+++ b/lib/index.js
@@ -926,7 +926,7 @@ function imageMediaTypeForPath(filePath) {
 /**
-* Enforce the strict image-capability gate for the calling route. Resolves the
-* session's latest routed provider/model (request header config, then agent
-* options) and requires the exact resolved route to declare `image` input explicitly.
+* Enforce the strict image-capability gate for the calling route first. A known
+* text-only route may ask composition for a fallback, whose exact model must
+* also declare `image` input before the tool proceeds.
 * @param ctx - the plugin context used to resolve the optional `llm` service.
 * @param exec - the tool-execution context supplying the calling agent.
 * @param requestedPath - the raw, not-yet-resolved path rendered in refusal messages.
@@ -940 +940,29 @@ async function assertImageCapableRoute(ctx, exec, requestedPath) {
-	if (active.inputModalities === void 0 || !active.inputModalities.includes("image")) throw new Error(`cannot read "${requestedPath}" as an image: model "${model}" does not declare image input; switch to an image-capable model to read images`);
+	if (active.inputModalities?.includes("image")) return;
+	const refusal = () => new Error(`cannot read "${requestedPath}" as an image: model "${model}" does not declare image input; switch to an image-capable model to read images`);
+	let fallback;
+	try {
+		fallback = await ctx.waterfall("fs/read-image-route", exec, {
+			provider,
+			model
+		}, () => Promise.resolve(void 0));
+	} catch {
+		exec.signal.throwIfAborted();
+		throw refusal();
+	}
+	if (fallback === void 0) throw refusal();
+	if (typeof fallback.provider !== "string" || fallback.provider.length === 0 || typeof fallback.model !== "string" || fallback.model.length === 0 || typeof fallback.activate !== "function") throw refusal();
+	let selected;
+	try {
+		selected = await llm.resolveModelInfo(fallback.provider, fallback.model, exec.signal);
+	} catch {
+		exec.signal.throwIfAborted();
+		throw refusal();
+	}
+	if (selected.inputModalities === void 0 || !selected.inputModalities.includes("image")) throw refusal();
+	return () => {
+		try {
+			fallback.activate();
+		} catch {
+			throw refusal();
+		}
+	};
@@ -1036 +1064 @@ function applyReadImageTool(ctx) {
-			await assertImageCapableRoute(ctx, exec, args.file_path);
+			const activateFallback = await assertImageCapableRoute(ctx, exec, args.file_path);
@@ -1060,0 +1089 @@ function applyReadImageTool(ctx) {
+			activateFallback?.();
diff --git a/lib/types/index.d.ts b/lib/types/index.d.ts
index b4f19db..86d0f04 100644
--- a/lib/types/index.d.ts
+++ b/lib/types/index.d.ts
@@ -6,6 +6,7 @@
  */
 import type { Context } from '@deepseek-ai/cordis';
 import z from '@deepseek-ai/schemastery';
+export type { ImageRouteFallback } from './read-image.js';
 /** Cordis plugin name used by loader diagnostics. */
 export declare const name = "tool-fs";
 /** Services required by the filesystem tool suite. */
diff --git a/lib/types/read-image.d.ts b/lib/types/read-image.d.ts
index e77a240..670af34 100644
--- a/lib/types/read-image.d.ts
+++ b/lib/types/read-image.d.ts
@@ -7,8 +7,28 @@
  * failure after filesystem and attachment work.
  * @module @deepseek-ai/dsh-tool-fs/src/read-image
  */
 import type { Context } from '@deepseek-ai/cordis';
 import type { ImageAttachmentRef, ImageMediaType } from '@deepseek-ai/dsh-attachment';
 import type { ToolExecution } from '@deepseek-ai/dsh-tools';
+/** Exact route a composition may nominate after the calling route rejects image input. */
+export interface ImageRouteFallback {
+    provider: string;
+    model: string;
+    /** Commit routing state after this route passes validation and the image is durably stored. */
+    activate(): void;
+}
+declare module '@deepseek-ai/cordis' {
+    interface Events {
+        /**
+         * Optionally nominate a route for one `read_image` call whose calling
+         * model is known text-only. The tool validates the returned exact route
+         * with the same fail-closed modality rule before invoking `activate`.
+         */
+        'fs/read-image-route'(exec: ToolExecution, current: {
+            provider: string;
+            model: string;
+        }, next: () => Promise<ImageRouteFallback | undefined>): Promise<ImageRouteFallback | undefined>;
+    }
+}
 /** The structured outcome declared by the `read_image` output schema. */
 export interface ImageReadValue {
@@ -56,7 +76,7 @@ export declare function imageMediaTypeForPath(filePath: string): ImageMediaType
 /**
- * Enforce the strict image-capability gate for the calling route. Resolves the
- * session's latest routed provider/model (request header config, then agent
- * options) and requires the exact resolved route to declare `image` input explicitly.
+ * Enforce the strict image-capability gate for the calling route first. A known
+ * text-only route may ask composition for a fallback, whose exact model must
+ * also declare `image` input before the tool proceeds.
  * @param ctx - the plugin context used to resolve the optional `llm` service.
  * @param exec - the tool-execution context supplying the calling agent.
  * @param requestedPath - the raw, not-yet-resolved path rendered in refusal messages.
@@ -63,5 +83,5 @@ export declare function imageMediaTypeForPath(filePath: string): ImageMediaType
  */
-export declare function assertImageCapableRoute(ctx: Context, exec: ToolExecution, requestedPath: string): Promise<void>;
+export declare function assertImageCapableRoute(ctx: Context, exec: ToolExecution, requestedPath: string): Promise<void | (() => void)>;
 /**
  * Re-brand a structured image outcome into the durable attachment reference an
  * `ImageBlock` carries.
